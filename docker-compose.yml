version: "3.0"

services:
  postgres:
    image: postgres:14-alpine
    container_name: postgres_hd
    environment:
      POSTGRES_DB: "hd_db"
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "postgres"
    volumes:
      - .:/var/data
    ports:
      - "5432:5432"