drop table if exists result cascade;

create table result(
  id int8 GENERATED BY DEFAULT AS IDENTITY,
  user_id int8,
  event_id int8,
  points int,
  constraint fk_user_result foreign key (user_id) references usr(id),
  constraint fk_event_result foreign key (event_id) references event(id),
  PRIMARY KEY (id)
);